{% set version = '1.40-1' %}

{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-compositions
  version: {{ version|replace("-", "_") }}

source:
  fn: compositions_{{ version }}.tar.gz
  url:
    - https://cran.r-project.org/src/contrib/compositions_{{ version }}.tar.gz
    - https://cran.r-project.org/src/contrib/Archive/compositions/compositions_{{ version }}.tar.gz


  sha256: 0bc1e16b47fd87be7f8896dcfc3ae8640b028270494e23dbc0812d835e1ac1c2
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build number.
  number: 0

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/

# Suggests: rgl
requirements:
  build:
    - r-base
    - r-bayesm
    - r-energy
    - r-robustbase
    - r-tensora
    - posix                # [win]
    - {{native}}toolchain  # [win]
    - gcc                  # [not win]

  run:
    - r-base
    - r-bayesm
    - r-energy
    - r-robustbase
    - r-tensora{indent}libgcc  # [not win]

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('compositions')"  # [not win]
    - "\"%R%\" -e \"library('compositions')\""  # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http
  license: GPL (>= 2)
  summary: The package provides functions for the consistent analysis of compositional data (e.g.
    portions of substances) and positive numbers (e.g. concentrations) in the way proposed
    by Aitchison and Pawlowsky-Glahn.
  license_family: GPL3

# The original CRAN metadata for this package was:

# Package: compositions
# Version: 1.40-1
# Date: 2013-06-01
# Title: Compositional Data Analysis
# Author: K. Gerald van den Boogaart <boogaart@hzdr.de>,  Raimon Tolosana, Matevz Bren
# Maintainer: K. Gerald van den Boogaart <support@boogaart.de>
# Depends: R (>= 2.2.0), tensorA, robustbase, energy, bayesm
# Suggests: rgl
# Description: The package provides functions for the consistent analysis of compositional data (e.g. portions of substances) and positive numbers (e.g. concentrations) in the way proposed by Aitchison and Pawlowsky-Glahn.
# License: GPL (>= 2)
# URL: http://www.stat.boogaart.de/compositions
# Packaged: 2014-06-06 15:58:33 UTC; tolosa53
# NeedsCompilation: yes
# Repository: CRAN
# Date/Publication: 2014-06-07 21:11:09

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
